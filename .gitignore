# Google OAuth client secret — must never be committed
secrets/

# Ad-hoc API dumps from debugging sessions; may contain tokens
response*.json

# Supabase CLI scratch state — machine-local, and pooler-url is a Postgres
# connection string
supabase/.temp/

.DS_Store
.vscode/

# One-off Drive migration state — holds OAuth refresh tokens
tools/.migration/
