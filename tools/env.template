# Copy this to .migration/env and fill it in. That file is git-ignored.
# Nothing here should ever be pasted into a chat or a commit.

# --- OLD: the personal Gmail account's project ---
# Must be the ORIGINAL client id. drive.file access is per (client_id, user),
# so a new client authorising the same Gmail account would see no files at all.
OLD_CLIENT_ID=
OLD_CLIENT_SECRET=
# Leave blank and run `find-folder` to discover it.
OLD_FOLDER_ID=

# --- NEW: adcock-botanical-garden-app, in the justamanda.net org ---
NEW_CLIENT_ID=
NEW_CLIENT_SECRET=
NEW_FOLDER_NAME=Adcock Botanical Garden Photos
